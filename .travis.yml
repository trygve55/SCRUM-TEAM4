language: node_js
node_js:
  - "node"

notifications:
  email: false

branches:
  only:
  - master

script:
  - npm test

after_success:
  - bash <(curl -s https://codecov.io/bash)

services:
  - mysql

before_install:
  - mysql -e 'CREATE DATABASE test;'